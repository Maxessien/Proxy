# Accept traffic on the port provided by Render
http_port MY_RENDER_PORT

# Basic access control (Allows everyone - secure this later!)
acl allsrc src all
http_access allow allsrc

# Log to stdout so you can see logs in the Render dashboard
logfile_rotate 0
access_log stdio:/dev/stdout
cache_log /dev/stdout
